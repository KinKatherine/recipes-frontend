import { describe, it, expect } from 'vitest';

// Тест-заглушка, чтобы Vitest не ругался на пустой файл
describe('Format Utils', () => {
  it('should have a placeholder test', () => {
    expect(true).toBe(true);
  });
});
